dependencies {
    compile project(':test-common')

    compile group: 'com.google.guava', name: 'guava', version: guavaVersion
    compile group: 'org.ejml', name: 'ejml-simple', version: '0.37.1'
}
